name: Security
on:
  schedule: [{ cron: "0 3 * * 1" }]
  workflow_dispatch: {}
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Add CodeQL/Trivy as an extension."
